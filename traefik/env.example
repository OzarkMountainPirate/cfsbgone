# Copy this file to .env and fill in your real Cloudflare API token.
# Token scope: Zone : DNS : Edit  (for the zone hosting your VPS hostname).
# NEVER commit the .env file with a real token. The .gitignore excludes it.

CF_DNS_API_TOKEN=your_cloudflare_api_token_here
